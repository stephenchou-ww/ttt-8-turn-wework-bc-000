#!/usr/bin/env ruby
require_relative "../lib/turn.rb"

puts "Welcome to Tic Tac Toe!"

board = Array.new(9, " ")
display_board(board)

turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)

